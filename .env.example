# Required for stdio transport only
# For HTTP transport, users provide their own credentials at login time
FT_CLIENT_ID=your_client_uid_from_profile.intra.42.fr
FT_CLIENT_SECRET=your_client_secret

# Optional — defaults to 8080 (Railway injects this automatically)
# PORT=8080
